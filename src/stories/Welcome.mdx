import { Meta } from '@storybook/blocks';

<Meta title='Welcome' />

{/* ---------- HERO ---------- */}

<div style={{
	position: 'relative',
	background: 'radial-gradient(ellipse at top right, #1A4A6B 0%, #092E44 60%)',
	color: '#FFFFFF',
	padding: '72px 56px 64px',
	borderRadius: 20,
	marginBottom: 40,
	overflow: 'hidden',
	boxShadow: '0 20px 60px rgba(9, 46, 68, 0.25)',
}}>
	{/* Decorative grid */}
	<div style={{
		position: 'absolute',
		top: 0, left: 0, right: 0, bottom: 0,
		backgroundImage: 'linear-gradient(rgba(255,255,255,0.04) 1px, transparent 1px), linear-gradient(90deg, rgba(255,255,255,0.04) 1px, transparent 1px)',
		backgroundSize: '32px 32px',
		pointerEvents: 'none',
	}} />

	<div style={{ position: 'relative' }}>
		<div style={{ display: 'inline-flex', alignItems: 'center', gap: 8, padding: '4px 10px', borderRadius: 999, background: 'rgba(255,255,255,0.1)', border: '1px solid rgba(255,255,255,0.18)', fontSize: 11, letterSpacing: 1.5, color: '#FFFFFF', marginBottom: 20 }}>
			<span style={{ width: 6, height: 6, borderRadius: 999, background: '#10B981' }} />
			FLEXPRICE DESIGN SYSTEM · v1.0
		</div>

		<h1 style={{ fontSize: 52, fontWeight: 700, lineHeight: 1.05, margin: 0, marginBottom: 18, letterSpacing: '-0.02em', color: '#FFFFFF' }}>
			The component library<br />
			<span style={{ color: '#7DD3FC' }}>powering Flexprice billing.</span>
		</h1>

		<p style={{ fontSize: 17, lineHeight: 1.6, color: 'rgba(255,255,255,0.82)', maxWidth: 640, margin: 0 }}>
			Atoms, molecules and organisms used across the dashboard — usage metering, credit
			management, pricing tiers and invoicing UIs. Built on the existing Flexprice palette,
			with motion polish that keeps the same color tokens.
		</p>

		<div style={{ display: 'flex', gap: 12, marginTop: 28 }}>
			<div style={{ display: 'inline-flex', alignItems: 'center', gap: 8, padding: '10px 16px', borderRadius: 999, background: '#FFFFFF', color: '#092E44', fontSize: 13, fontWeight: 600 }}>
				Showcase / Dashboard Scenes →
			</div>
			<div style={{ display: 'inline-flex', alignItems: 'center', gap: 8, padding: '10px 16px', borderRadius: 999, background: 'transparent', color: '#FFFFFF', border: '1px solid rgba(255,255,255,0.3)', fontSize: 13, fontWeight: 500 }}>
				Design Tokens →
			</div>
		</div>
	</div>
</div>

{/* ---------- STATS ---------- */}

<div style={{ display: 'grid', gridTemplateColumns: 'repeat(4, 1fr)', gap: 12, marginBottom: 40 }}>
	{[
		{ n: '27', label: 'Components', sub: 'across atoms, molecules, organisms' },
		{ n: '60+', label: 'Stories', sub: 'with full argTypes & docs' },
		{ n: '64', label: 'Tests', sub: 'utility + component + interaction' },
		{ n: '3 / 3', label: 'Challenges', sub: 'A · B · C all complete' },
	].map((s) => (
		<div key={s.label} style={{ position: 'relative', padding: 20, border: '1px solid #E4E4E7', borderRadius: 12, background: '#FFFFFF' }}>
			<div style={{ position: 'absolute', top: 16, right: 16, width: 6, height: 6, borderRadius: 999, background: '#10B981' }} />
			<div style={{ fontSize: 36, fontWeight: 700, color: '#092E44', lineHeight: 1, letterSpacing: '-0.02em' }}>{s.n}</div>
			<div style={{ fontSize: 13, fontWeight: 600, color: '#18181B', marginTop: 10 }}>{s.label}</div>
			<div style={{ fontSize: 11, color: '#71717A', marginTop: 4, lineHeight: 1.5 }}>{s.sub}</div>
		</div>
	))}
</div>

{/* ---------- WHAT MAKES IT TICK ---------- */}

<div style={{ marginBottom: 40 }}>
	<div style={{ fontSize: 12, letterSpacing: 2, color: '#71717A', marginBottom: 8 }}>WHAT MAKES IT TICK</div>
	<h2 style={{ fontSize: 24, fontWeight: 600, color: '#092E44', margin: 0, marginBottom: 20 }}>Four design principles, applied everywhere.</h2>

	<div style={{ display: 'grid', gridTemplateColumns: 'repeat(2, 1fr)', gap: 12 }}>
		{[
			{ icon: '◐', title: 'One palette, everywhere', body: 'Navy #092E44 for primary, zinc grays for surfaces. Zero new colors — every component, animation and showcase reuses the same tokens.' },
			{ icon: '◇', title: 'Composable, not monolithic', body: 'Atoms compose into molecules. Molecules into organisms. Each layer ships its own stories so reviewers can see the building blocks alone or in context.' },
			{ icon: '◈', title: 'Tested in isolation', body: 'Every interactive component carries a Storybook play function — clicks, typing, state assertions run on every page load. 64 tests beyond that.' },
			{ icon: '◆', title: 'Motion with intent', body: 'framer-motion springs on toggles, count-up tweens on counters, staggered row entries on tables. Subtle, never gimmicky, always respecting prefers-reduced-motion.' },
		].map((p) => (
			<div key={p.title} style={{ padding: 18, border: '1px solid #E4E4E7', borderRadius: 12, background: '#FFFFFF' }}>
				<div style={{ display: 'flex', alignItems: 'flex-start', gap: 12 }}>
					<div style={{ display: 'flex', alignItems: 'center', justifyContent: 'center', width: 32, height: 32, borderRadius: 8, background: '#092E44', color: '#FFFFFF', fontSize: 16, fontWeight: 700, flexShrink: 0 }}>{p.icon}</div>
					<div>
						<div style={{ fontSize: 14, fontWeight: 600, color: '#18181B', marginBottom: 4 }}>{p.title}</div>
						<div style={{ fontSize: 12, color: '#71717A', lineHeight: 1.6 }}>{p.body}</div>
					</div>
				</div>
			</div>
		))}
	</div>
</div>

{/* ---------- WHERE TO START ---------- */}

<div style={{ marginBottom: 40 }}>
	<div style={{ fontSize: 12, letterSpacing: 2, color: '#71717A', marginBottom: 8 }}>WHERE TO START</div>
	<h2 style={{ fontSize: 24, fontWeight: 600, color: '#092E44', margin: 0, marginBottom: 20 }}>Three paths through this Storybook.</h2>

	<div style={{ display: 'flex', flexDirection: 'column', gap: 10 }}>
		{[
			{ tag: '01', title: 'Showcase / Dashboard Scenes', body: 'Composite scenes — Invoices Page, Billing Page, Metrics Dashboard, Onboarding Wizard. The fastest way to see how everything fits together.' },
			{ tag: '02', title: 'Showcase / Challenge A — Filter Store', body: 'DataTable wired to a Zustand store with sessionStorage persistence and a URL fingerprint. Reload the iframe — filters survive.' },
			{ tag: '03', title: 'Molecules / DataTable / Virtualized10k', body: '10,000 mock rows rendered with @tanstack/react-virtual. Only ~12 in the DOM at any time. Scroll to feel it.' },
			{ tag: '04', title: 'Design Tokens', body: 'The color, type, spacing and elevation primitives every component is built on.' },
		].map((p) => (
			<div key={p.tag} style={{ display: 'flex', alignItems: 'flex-start', gap: 16, padding: 16, border: '1px solid #E4E4E7', borderRadius: 10, background: '#FFFFFF' }}>
				<div style={{ fontSize: 11, fontFamily: 'JetBrains Mono, monospace', color: '#A1A1AA', fontWeight: 600, minWidth: 24 }}>{p.tag}</div>
				<div style={{ flex: 1 }}>
					<div style={{ fontSize: 14, fontWeight: 600, color: '#092E44', marginBottom: 2 }}>{p.title}</div>
					<div style={{ fontSize: 12, color: '#71717A', lineHeight: 1.6 }}>{p.body}</div>
				</div>
				<div style={{ fontSize: 18, color: '#A1A1AA' }}>→</div>
			</div>
		))}
	</div>
</div>

{/* ---------- HIGHLIGHTS ---------- */}

<div style={{ marginBottom: 40 }}>
	<div style={{ fontSize: 12, letterSpacing: 2, color: '#71717A', marginBottom: 8 }}>WORTH YOUR ATTENTION</div>
	<h2 style={{ fontSize: 24, fontWeight: 600, color: '#092E44', margin: 0, marginBottom: 20 }}>Things to look at first.</h2>

	<div style={{ display: 'grid', gridTemplateColumns: 'repeat(3, 1fr)', gap: 12 }}>
		{[
			{ kicker: 'POLISH', title: 'PolishedToggle', body: 'Pill toggle with framer-motion spring physics. Same palette as the existing Toggle.' },
			{ kicker: 'POLISH', title: 'StatusBadge', body: 'Stripe-style pulsing dot for pending and processing states.' },
			{ kicker: 'POLISH', title: 'AnimatedCounter', body: 'Count-up KPI numbers with delta indicator. Used on the Metrics dashboard.' },
			{ kicker: 'CHALLENGE B', title: 'Virtualized DataTable', body: '10k rows, smooth scroll, ~12 in DOM. Configurable estimatedRowHeight.' },
			{ kicker: 'CHALLENGE C', title: 'Query Config', body: 'REALTIME / DEFAULT / STATIC presets + createQueryConfig helper. 14 tests.' },
			{ kicker: 'CHALLENGE A', title: 'Filter Store', body: 'Zustand factory keyed by route, sessionStorage persisted, 6-char URL fingerprint.' },
		].map((h) => (
			<div key={h.title} style={{ padding: 16, border: '1px solid #E4E4E7', borderRadius: 10, background: '#FAFAFA' }}>
				<div style={{ display: 'inline-flex', padding: '2px 8px', borderRadius: 4, background: '#092E44', color: '#FFFFFF', fontSize: 9, fontWeight: 700, letterSpacing: 1, marginBottom: 8 }}>{h.kicker}</div>
				<div style={{ fontSize: 13, fontWeight: 600, color: '#18181B', marginBottom: 4 }}>{h.title}</div>
				<div style={{ fontSize: 11, color: '#71717A', lineHeight: 1.5 }}>{h.body}</div>
			</div>
		))}
	</div>
</div>

{/* ---------- FOOTER ---------- */}

<div style={{ marginTop: 56, paddingTop: 24, borderTop: '1px solid #E4E4E7', display: 'flex', justifyContent: 'space-between', alignItems: 'center', fontSize: 12, color: '#71717A' }}>
	<div>
		Built with <strong style={{ color: '#18181B' }}>React 18</strong> · TypeScript · Vite · Tailwind · Radix UI · Storybook 8 · framer-motion · TanStack
	</div>
	<div style={{ fontFamily: 'JetBrains Mono, monospace', fontSize: 11 }}>by Vasudha Jain</div>
</div>
